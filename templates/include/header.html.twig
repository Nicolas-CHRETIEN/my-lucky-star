<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor02">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path('home')}}">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_stars')}}">Etoiles disponibles</a>
				</li>
				<li class="nav-item">
				{% if app.user %}
					<a class="nav-link" href="{{path('app_logout')}}">Se déconnecter</a>
				{% else %}
					<a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
				{% endif %}
				</li>
				{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_myAccount')}}">Mon compte</a>
				</li>
				{% endif %}
				{% if app.user %}
				<li class="nav-item">
					<a class="nav-link" href="{{path('star_create')}}">Ajouter une étoile</a>
				</li>
				{% endif %}
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Les types d'étoile</a>
					<div class="dropdown-menu">
						<a class="dropdown-item" href="{{path('app_category')}}">Les catégories</a>
						<a class="dropdown-item" href="{{path('app_concept')}}">Le concept</a>
						<a class="dropdown-item" href="{{path('app_lifeDuration')}}">La durée de vie</a>
						<div class="dropdown-divider">
						<a class="dropdown-item" href="{{path('app_lifeEnd')}}">La fin de vie</a>
                        </div>
					</div>
				</li>
			</ul>
		</div>
	</div>
</nav>